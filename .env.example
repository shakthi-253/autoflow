# AutoFlow has two separate .env files - one per service. See:
#   backend/.env.example   (DATABASE_URL, CORS_ORIGINS, DEBUG)
#   frontend/.env.example  (VITE_API_URL)
#
# Copy each to backend/.env and frontend/.env respectively before running.
